{
  "name": "rota-trip-finder-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/smoke.test.mjs && node test/e2e.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
